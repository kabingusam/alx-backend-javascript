import {Teacher} from "./Teacher";

class React extends Subject{
    getRequirements() {
        return "Here is the list of requirements for React"
    }
    getAvailableTeacher() {
        // the teacher firstname
        return `Available Teacher`
    }
        // fix the return no teacher 
    if(experienceTeachingReact: number) {
        
    }
}