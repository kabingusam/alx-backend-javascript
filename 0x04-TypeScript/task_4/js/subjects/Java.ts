class Java {
    getRequirements() {
        return "Here is the list of requirements for Java"
    }
    // fix this to return the available teacher
    getAvailableTeacher() {
        return `Available Teachers: ${this.getAvailableTeacher}`
    }
    // fix this to return the message if experience = 0
    if(experienceTeachingReact: number) {
        return "No available teacher"
    }
}